<div align="center">

# 一、 🚀 本地部屬

</div>


## ⚙️ 硬件要求

- 最低配置為 2 核 CPU + `4GB` 內存（測試環境）
- 生產環境建議 4 核 CPU + `16GB` 內存 + 100GB SSD

**如果打算做完全本地部署（不用雲端大模型）：**
- Windows 電腦建議至少 16GB 系統內存（推薦 `24GB 以上`）
- Mac 建議 Apple Silicon（M1/M2/M3/M4）至少 16GB 統一內存（推薦 `32GB`）。

## ⚙️ Docker Compose 部署流程

首先需要安裝並執行 Docker Desktop。通過以下步驟進行部署：

1. 克隆 Dify 源碼：`git clone https://github.com/langgenius/dify.git`
2. 進入 Docker 目錄：`cd dify/docker`
3. 複製環境配置文件：`cp .env.example .env`
4. 啟動服務：`docker compose up -d`
5. 訪問控制台：`http://localhost/install` 完成管理員初始化

Windows 需啟用 WSL2，macOS 建議分配 8GB Docker 內存。部署完成後，可以看到多個容器正在運行，包括 nginx、api、worker、web 等組件。

<div align="center">

# 二、 ☁️雲端版本

</div>

> 網址：[https://dify.ai/](https://dify.ai/)

Dify 提供零設定的雲服務，包括自託管版本的所有功能。Sandbox 計劃提供 200 次免費的 GPT-3.5、GPT-4、GPT-o4 呼叫以供試驗（教育版 5000 次/月），直接註冊即可使用。

![價格方案](/Dify/Image/img01_price01.png)

## ✨Dify 雲端教育版方案

> 介紹文本：https://docs.dify.ai/zh-hans/getting-started/dify-for-education

For 學生、教師、教育機構。驗證過後，可獲得一年免費 Profession（價值 $708 美金）方案存取權。

1. 申請流程：
  - 設定 > Billing > 教育版認證
  - 輸入身分與學校**全名**
  - 申請完畢後會直接將 100% 折扣券加入到帳號中
    
2. 激活訂閱流程：
   - 主頁 > 升級按鈕 > 點選右上角的年度計費 > 選擇 Professional 方案進行付費
   - 進到頁面中，會看到已經有優惠碼折扣
   - 再回到主頁，就會看見已經有顯示`🎓PRO`的圖示
   - 他會自動幫你取消下一期的訂閱（所以不用擔心一年過了之後會被收費）
   - 如果要確認是否取消，可以從 主頁 > `🎓PRO` > 管理賬單及訂閱
  
3. 限制：
   - 每年需要申請一次教育版優惠（只要 mail 有 edu 即可，不需要在學證明）
   - 用戶需在有效期結束前續期，過期權益即失效
   - 教育版訂閱只能用於該 Workspace Owner 的空間

## ⚙️ Dify 操作流程詳解

### 初始設置

1. 帳號登入

  - 可以利用 Google、GitHub，或是電子信箱登入。

2. 模型設置

> 個人帳戶圖片 > 設置 > 模型供應商

- 安裝需要的 API 廠商（Dify 支援的模型包括 OpenAI、Gemini、Claude，以及兼容 OpenAI 的模型。）
- 配置`系統模型設定`

![模型設定](/Dify/Image/img03_model-set.png)

3. 模型類型介紹

Dify 將模型分為 4 種類型，每種類型都有不同的用途：
  
#### 1️⃣ 系統推理模型

> 用於聊天、名稱生成和建議後續問題等任務的應用程序。

提供商包括：OpenAI、Azure OpenAI Service、Anthropic、Hugging Face Hub、Replicate、Xinference、OpenLLM、 科大訊飛星火、 文心一言 、TONGYI、Minimax、ZHIPU（ChatGLM）、Ollama、LocalAI、GPUStack。

#### 2️⃣ 嵌入模型

> 用於將分段文件嵌入知識中並處理應用程式中的使用者查詢。

提供商包括 OpenAI、ZHIPU （ChatGLM）、Jina AI（Jina Embeddings）。

#### 3️⃣ 重新排名模型

> 增強 LLM 中的搜索能力。

提供商包括 Cohere、Jina AI（Jina Reranker）。

#### 4️⃣ 語音轉文字模型

> 在對話式應用程式中將口語轉換為文字。

提供者：OpenAI。

<div align="center">

# 3. 💼企業版

</div>

如果是企業，Dify還提供額外功能，並支援在 `AWS Marketplace` 上一鍵部署至自己的 AWS VPC。

![企業方案](/Dify/Image/img02_price02.png)



